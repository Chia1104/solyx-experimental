import { DefiPlaceholderScreen } from '@/modules/defi/components/defi-placeholder-screen';

export default function AddAccountScreen() {
  return (
    <DefiPlaceholderScreen
      description="Add account route for deriving a new wallet index from the stored mnemonic and registering supported chain addresses."
      title="Add Account"
    />
  );
}
