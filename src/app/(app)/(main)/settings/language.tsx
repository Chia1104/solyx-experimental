import { DefiPlaceholderScreen } from '@/modules/defi/components/defi-placeholder-screen';

export default function SettingLanguageScreen() {
  return (
    <DefiPlaceholderScreen
      description="Language settings route. It should call changeLanguageCode and keep i18next resources in sync."
      title="Language"
    />
  );
}
