import { DefiPlaceholderScreen } from '@/modules/defi/components/defi-placeholder-screen';

export default function SettingAccountScreen() {
  return (
    <DefiPlaceholderScreen
      description="Account settings route for CEX account info, identity state, and linked contact methods."
      title="Account Settings"
    />
  );
}
