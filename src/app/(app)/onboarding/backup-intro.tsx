import { BackupIntroScreen } from '@/components/backup/backup-intro-screen';

export default function BackupIntro() {
  return <BackupIntroScreen isBrandVisible phraseHref="/onboarding/backup-phrase" />;
}
