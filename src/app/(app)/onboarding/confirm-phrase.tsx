import { ConfirmPhraseScreen } from '@/components/backup/confirm-phrase-screen';

export default function ConfirmPhrase() {
  return <ConfirmPhraseScreen completeHref="/onboarding/done" isBrandVisible />;
}
