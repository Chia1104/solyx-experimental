import { BackupPhraseScreen } from '@/components/backup/backup-phrase-screen';

export default function BackupPhrase() {
  return <BackupPhraseScreen confirmHref="/onboarding/confirm-phrase" isBrandVisible />;
}
